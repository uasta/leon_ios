# 03｜核心用户流程（从首次打开到日常使用）

## Flow A：首次打开（不登录也能完成）

目标：10 分钟内让用户感知价值。

- 打开 App → 看到「食材」列表空状态
- 空状态引导：
  - 主按钮：**添加第一个食材**
  - 次按钮：导入/示例（可选）
- 新增食材 Sheet：
  - 名称（可快捷选“常用食材”）
  - 到期日（可选，但推荐填）
  - 位置（默认值）
- 添加 ≥3 个后：
  - 提示开启通知（系统权限弹窗前先做自解释）
  - 引导设置“临期提前 N 天”